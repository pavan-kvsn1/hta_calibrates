apiVersion: v1
kind: ServiceAccount
metadata:
  name: hta-app
  namespace: hta-calibration
  labels:
    app.kubernetes.io/name: hta-calibration
    app.kubernetes.io/part-of: hta-calibration
  annotations:
    # Workload Identity - MUST be overridden in overlay patches
    iam.gke.io/gcp-service-account: REPLACE_IN_OVERLAY@PROJECT.iam.gserviceaccount.com
